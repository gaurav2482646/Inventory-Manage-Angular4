import {InventorySearchPipe} from './inventory-search.pipe';

describe('InventorySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new InventorySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
